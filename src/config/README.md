# Configuration

Управление настройками приложения для разных окружений (Dev, Stage, Prod).

## Здесь
- Загрузка переменных окружения (`.env`).
- Валидация настроек при старте приложения (через Pydantic Settings).
- Хранение констант и feature-flags.
